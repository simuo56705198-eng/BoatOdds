# === Core ===
streamlit>=1.30.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
pandas>=1.5.0

# === Note ===
# pandas は dashboard.py が直接 import するため明示的に指定。
# streamlit の依存として自動インストールされるが、明示が安全。
#
# === 開発時のみ（テスト実行） ===
# pip install pytest pytest-cov
